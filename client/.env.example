# Client Environment Configuration
# Optional: Set this only if deploying frontend and backend to separate domains (e.g. Vercel + Render)
# For unified single-service deployments, leave empty (defaults to '/api')
VITE_API_URL=
